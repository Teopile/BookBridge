# Render Static Site routing — SPA fallback only.
# (External-URL rewrites aren't supported on Render Free tier; SPAs call the
# API cross-origin instead. CSRF token is exchanged via the x-csrf-token
# response header — see admin/src/api.js.)
/*  /index.html  200
