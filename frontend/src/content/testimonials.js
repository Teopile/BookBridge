// Community quotes for „რას ამბობს ჩვენი საზოგადოება" (What our community says)
// on the About page.
//
// DATA-DRIVEN + SHIPS DARK: while this array is empty the section does not
// render. Adding a record = a card appears — zero code changes.
// Deliberately NO star ratings — this is a donation platform, not a store.
//
// Record shape:
//   id     unique slug
//   name   { ka, en }   first name (+ optional role, e.g. "დონორი")
//   quote  { ka, en }
//   date   ISO string shown as a plain date
//
// TODO(content): waiting on real community quotes from the owner.
export default [];
